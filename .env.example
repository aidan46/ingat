# Copy to .env and fill in. .env is gitignored; this file is tracked.

# Postgres connection string (matches docker-compose.yml; not a secret, local creds)
DATABASE_URL="postgresql://ingat:ingat@localhost:5432/ingat?schema=public"

# Runtime LLM credential, read only by lib/anthropic.ts
ANTHROPIC_API_KEY=""
